# macOS
.DS_Store

# Local-only overrides (secrets, machine-specific) — never commit these
*.local
.zshrc.local
.gitconfig.local

# Editor/OS noise
*.swp
*~

# Python
__pycache__/
*.pyc

# Neovim generated build state
nvim/.config/nvim/plugin/packer_compiled.lua
